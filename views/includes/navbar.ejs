<nav class="navbar border-bottom navbar-expand-md bg-white sticky-top shadow-sm">
  <div class="container-fluid px-md-4 position-relative">
    
    <!-- 1. Left Section: Logo + Explore Homes -->
    <div class="d-flex align-items-center">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <img src="/images/smollLogo.png" alt="Atithi Logo" height="36" />
      </a>
      <a class="nav-link fw-medium text-dark ms-2 d-none d-md-block" href="/listings">Explore Homes</a>
    </div>

    <!-- 2. Mobile Toggler -->
    <button
      class="navbar-toggler border-0 focus-ring ms-auto ms-md-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- 3. Desktop Search Bar (Dead Center) -->
    <div class="nav-search-wrapper d-none d-md-block position-absolute top-50 start-50 translate-middle">
      <form class="d-flex nav-search-form" action="/listings/search" method="GET">
        <input 
          class="form-control rounded-pill me-2 ps-3 search-input" 
          type="search" 
          name="q"
          placeholder="Search Destinations or Cultures..." 
          aria-label="Search"
          required 
        />
        <button class="btn btn-search rounded-pill px-4" type="submit">Search</button>
      </form>
    </div>

    <!-- 4. Right Section: Navigation Menu Links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto align-items-md-center gap-md-3">
        <a class="nav-link fw-medium d-md-none" href="/listings">Explore Homes</a>
        <a class="nav-link fw-medium" href="/listings/new">Host Your Home</a>
        
        <% if(!currUser) { %> 
          <a class="nav-link text-dark fw-semibold" href="/user/signup">Sign Up</a>
          <a class="nav-link text-dark fw-semibold" href="/user/login">Log In</a>
        <% } %>
        <% if(currUser) { %>
          <a class="nav-link text-dark fw-semibold" href="/user/logout">Log Out</a>
        <% } %>
      </div>
    </div>

  </div>

  <!-- 5. Mobile Search Bar Row (Hidden on Desktop) -->
  <div class="container-fluid d-block d-md-none px-3 pb-2 pt-1">
    <form class="d-flex w-100" action="/listings/search" method="GET">
      <input 
        class="form-control rounded-pill me-2 ps-3 search-input" 
        type="search" 
        name="q"
        placeholder="Search Destinations or Cultures..." 
        aria-label="Search"
        required 
      />
      <button class="btn btn-search rounded-pill px-4" type="submit">Search</button>
    </form>
  </div>
</nav>